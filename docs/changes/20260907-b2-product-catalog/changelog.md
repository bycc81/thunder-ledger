# B2 变更记录：商品资料与图片

## 2026-09-07

- 已确认产品与 UX 定义及可运行 HTML 原型。
- 页面/API 契约已并入 B2 技术设计，作为唯一技术契约。
- Render 与 R2 已由用户完成配置；B2 技术设计和测试计划已纳入工作区图片授权、关联、首图、轮播与大图。
- 库存批次、数量、成本和库存流水属于后续独立模块。
- 已新增 B2 商品、工作区资源归属与图片顺序迁移，以及 Fastify 商品/图片 API 和移动端商品页面；本地迁移因既有 `002_batch1_access` 校验和不一致停止，尚未写入 B2 表。
- 已安全核对并修复本地 `002`、`003` 的历史迁移校验记录；发现 `001` 已记录但缺少 `assets` 表，004 已补充幂等基础表创建后重新验证。
- 本地 `004_product_catalog` 已成功执行，Docker 已重建；健康、就绪、商品列表 API 和商品路由冒烟检查通过。真实 R2 上传和移动端视觉验收待 Render/Pages 部署后执行。
- R2 联通探针确认当前本地 Compose 未注入 `R2_ENDPOINT`、Bucket 和密钥，返回 `STORAGE_NOT_CONFIGURED`；Render Secret 不会自动同步到本机。
- 本地 R2 配置后，签名接口成功，但实际对象 PUT/DELETE 均返回 `403 AccessDenied`；待 R2 API Token 补齐目标 Bucket 对象读写权限后重试，失败测试没有留下对象。
- 已修正本地 Bucket 名中的 Unicode 连字符；R2 服务器链路（签名、PUT、完成、私有读取、DELETE）现已通过。浏览器 CORS 预检仍为 403，待修正 Bucket CORS 后验收商品页上传。
- 重试确认 R2 返回 `CORS not configured for this bucket`，当前命中的 Bucket 尚未生效 CORS 配置；服务器侧读写不受影响。
- CORS 配置生效后，本地与 Pages 预检均返回 204；完整 R2 链路（预检、上传、完成、私有读取、清理）全部通过。
## 2026-09-08

- 将 B2 收口为商品资料与图片的单一交付模块；库存批次、数量、成本和库存流水改列后续独立模块。
- 修复商品表单上传完成后仍停留在“图片上传中”的状态同步问题；上传完成后会更新响应式图片项，保存按钮随之恢复可用。
- 调整商品信息架构：商品进入底部四项主导航并在商品页高亮；操作记录保留为工作区内的管理入口。同步收紧顶栏高度、品牌层级和左右边距。
- 工作区概览的快捷操作改用商品、成员、批次和记录的线性图标，移除中文方块缩写。
- 补齐 Vant Icon 全局注册，确保快捷操作及既有页面图标实际渲染。
- 修正快捷操作布局：左侧图标与文案同组，右侧箭头独立；三者在单行内垂直居中。
- 补齐商品详情响应的 `firstImage`，使其与商品列表和轮播图片响应保持一致。
- 完成商品图片关联和工作区授权实测：首图、详情签名 URL、viewer 只读及跨工作区隔离均通过；临时权限测试数据已清理。
- 完成多图图片顺序实测：创建、首图重排和解除关联均符合预期，被解除关联的底层资产保留。
- 完成商品列表首图接口实测；云端部署联通与目标移动视口截图验收留待部署后执行。
- 完成 390×844 与 430×932 本地视觉及交互验收；仅剩云端部署后的 Render/Pages 联通 Smoke。
