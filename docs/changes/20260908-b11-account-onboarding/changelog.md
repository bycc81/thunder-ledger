# B1.1 注册与账号管理变更记录

## 2026-09-08

- 用户确认在 B4 前完成注册与账号管理。
- 确认用户名由邀请人设置，受邀人只设置密码。
- 产品与 UX 定义、HTML 原型及技术设计已确认，进入实现。
- 完成注册页、已有账号直接加入、本人修改密码、系统账号管理与会话失效版本；本地迁移 `009_account_session_version` 已执行，等待用户页面验收。
- 补齐会话恢复接口的会话版本校验和并发同名注册提示；已重建开发环境 API/Web 容器。
- 修复开发环境非 HTTPS 访问时，注册链接复制提示成功但剪贴板内容不可靠的问题；链接改为可长按选择，并使用兼容复制方式。
- 兼容复制仅在浏览器实际触发复制事件后提示成功；增加“打开链接”备用入口，避免剪贴板受限时无法取得注册链接。
- 修复创建注册链接后 Vant 默认确认行为自动关闭弹窗的问题；生成的链接会保留在弹窗内，直到用户主动关闭。
- 修复受邀注册完成时遗漏邀请编号、导致 PostgreSQL 返回 500 的问题；失败事务会完整回滚，原注册链接可重试。
- 修复页面刷新后未恢复登录信息的问题；普通用户不再显示为 `admin`，系统管理员会显示账号管理入口。
- 用户已验证已有账号可加入工作区。
- 账号管理页将普通账号的停用/恢复改为直接可见按钮；系统管理员账号仍保持不可操作。
- 用户已验证普通账号刷新后的用户名及系统管理员账号管理入口。
- 用户已验证账号停用/恢复、修改密码和旧登录状态失效；B1.1 收口，可进入 B4。
- 优化工作区体验：刷新页面保留上次选中的工作区；退出登录或该工作区不可用时清除并回退到可用工作区。
