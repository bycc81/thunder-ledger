# 测试与验收计划：移动端黄金页面

## 设计阶段

- 直接打开 `prototype/index.html`。
- 在 390×844 和 430×932 视口检查顶部栏、统计、列表、快捷操作和底部导航。
- 检查长工作区名、空批次、错误提示和无权限主操作。
- 扫描原型中的 `data-ai-id`，确认与注册表一致且重复项带业务 ID。

## 实现阶段

- `apps/web` 执行 `npm run typecheck` 和 `npm run build`。
- 阶段一当前结果：`WorkspaceOverview.vue` 已完成抽取，typecheck/build 均通过。
- 阶段一外壳修正结果：应用容器限制 480px、底部导航使用本地状态切换、主题色与黄金原型一致。
- 使用浏览器完成登录、切换工作区、新建批次、打开最近批次和底部导航切换。
- 确认当前工作区切换后所有数据请求使用新上下文。
- 通过截图对照原型，禁止以构建通过替代视觉验收。

## 完成门槛

截图、关键交互和 `data-ai-id` 三项全部通过，且用户确认黄金页面后，才进入下一页面实现。
